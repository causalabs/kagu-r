% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{KaguModel}
\alias{KaguModel}
\title{KaguModel — Bayesian graphical causal model}
\description{
The main user-facing class. Specify a DAG, optionally assign mechanisms
to nodes, fit to data, and extract causal effects.
}
\examples{
\dontrun{
library(kagu)

model <- KaguModel$new(
  dag = list(
    age     = c(),
    smoking = c("age"),
    health  = c("smoking", "age")
  )
)

model$fit(data)

effect <- model$effects("smoking", "health")
effect$summary()
}

}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{dag}}{Named list — the DAG specification.}

    \item{\code{mechanisms}}{Named list of \code{Mechanism} instances, one per node.}

    \item{\code{data}}{The training \code{data.frame} (set after \verb{$fit()}).}

    \item{\code{traces}}{Named list of \code{brmsfit} objects, one per node (after fit).}

    \item{\code{.fitted}}{Logical — whether \verb{$fit()} has been called.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-KaguModel-initialize}{\code{KaguModel$new()}}
    \item \href{#method-KaguModel-fit}{\code{KaguModel$fit()}}
    \item \href{#method-KaguModel-effects}{\code{KaguModel$effects()}}
    \item \href{#method-KaguModel-summary}{\code{KaguModel$summary()}}
    \item \href{#method-KaguModel-diagnostics}{\code{KaguModel$diagnostics()}}
    \item \href{#method-KaguModel-plot_dag}{\code{KaguModel$plot_dag()}}
    \item \href{#method-KaguModel-plot_posterior}{\code{KaguModel$plot_posterior()}}
    \item \href{#method-KaguModel-save}{\code{KaguModel$save()}}
    \item \href{#method-KaguModel-print}{\code{KaguModel$print()}}
    \item \href{#method-KaguModel-clone}{\code{KaguModel$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-initialize}{}}}
\subsection{\code{KaguModel$new()}}{
  Create a new KaguModel.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$new(dag, mechanisms = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{dag}}{Named list mapping each node to a character vector of its
parent node names. Root nodes map to \code{c()}.}
      \item{\code{mechanisms}}{Optional named list of \code{Mechanism} instances. Any
node not specified receives a \code{LinearMechanism} by default.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-fit"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-fit}{}}}
\subsection{\code{KaguModel$fit()}}{
  Fit each node's conditional distribution in topological order.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$fit(data, draws = 1000L, tune = 1000L, chains = 4L, ...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{A \code{data.frame} with one column per node.}
      \item{\code{draws}}{Integer — post-warmup draws per chain (default 1000).}
      \item{\code{tune}}{Integer — warmup draws per chain (default 1000).}
      \item{\code{chains}}{Integer — number of MCMC chains (default 4).}
      \item{\code{...}}{Additional arguments forwarded to \code{brms::brm()}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{self} invisibly (for method chaining).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-effects"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-effects}{}}}
\subsection{\code{KaguModel$effects()}}{
  Estimate the causal effect of \code{source} on \code{target}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$effects(
  source,
  target,
  values = NULL,
  std_units = FALSE,
  conditions = NULL,
  sweep = FALSE,
  sweep_n = 50L,
  sweep_range = NULL,
  hdi = 0.9
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{source}}{Character scalar — the intervention (treatment) node.}
      \item{\code{target}}{Character scalar — the outcome node.}
      \item{\code{values}}{Optional numeric vector of length 2 \code{c(from, to)} giving
the explicit intervention contrast.}
      \item{\code{std_units}}{Logical — if \code{TRUE}, compute the effect of a 1-SD
increase centred at the mean.}
      \item{\code{conditions}}{Optional named list of node values to condition on
(fixes those nodes at the given values during propagation).}
      \item{\code{sweep}}{Logical — if \code{TRUE}, compute the dose-response curve.}
      \item{\code{sweep_n}}{Integer — number of points in the sweep grid (default 50).}
      \item{\code{sweep_range}}{Numeric vector \code{c(min, max)} for the sweep grid.
Defaults to the observed range of \code{source}.}
      \item{\code{hdi}}{Numeric in (0, 1) — HDI probability (default 0.90).}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An \code{EffectResult} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-summary"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-summary}{}}}
\subsection{\code{KaguModel$summary()}}{
  Coefficient table across all node mechanisms.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$summary(hdi_prob = 0.9)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{hdi_prob}}{Numeric — HDI probability (default 0.90).}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{tibble} with columns \code{node}, \code{variable}, \code{mean}, \code{sd},
\code{hdi_lower}, \code{hdi_upper}, \code{rhat}, \code{ess_bulk}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-diagnostics"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-diagnostics}{}}}
\subsection{\code{KaguModel$diagnostics()}}{
  R-hat and ESS diagnostics for fitted nodes.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$diagnostics(node = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{node}}{Optional character scalar. If \code{NULL}, runs for all nodes.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{tibble} from \code{posterior::summarise_draws()}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-plot_dag"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-plot_dag}{}}}
\subsection{\code{KaguModel$plot_dag()}}{
  Visualise the DAG structure.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$plot_dag(node_pos = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{node_pos}}{Optional named list of \code{c(row, col)} grid coordinates
for manual node placement (overrides auto-layout for specified nodes).}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{ggplot} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-plot_posterior"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-plot_posterior}{}}}
\subsection{\code{KaguModel$plot_posterior()}}{
  Plot the posterior for a fitted node.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$plot_posterior(node)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{node}}{Character scalar — the node to plot.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{ggplot} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-save"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-save}{}}}
\subsection{\code{KaguModel$save()}}{
  Save the fitted model to disk.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$save(path, include_data = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{path}}{File path (e.g. \code{"model.rds"}).}
      \item{\code{include_data}}{Logical — whether to save the training data alongside
the model (default \code{TRUE}, so \verb{$effects()} works immediately on load).}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-print"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-print}{}}}
\subsection{\code{KaguModel$print()}}{
  Print a concise model summary.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$print(...)}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KaguModel-clone"></a>}}
\if{latex}{\out{\hypertarget{method-KaguModel-clone}{}}}
\subsection{\code{KaguModel$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{KaguModel$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
