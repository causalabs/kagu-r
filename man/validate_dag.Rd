% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dag.R
\name{validate_dag}
\alias{validate_dag}
\title{Validate a DAG specification}
\usage{
validate_dag(dag)
}
\arguments{
\item{dag}{Named list mapping each node name to a character vector of its
parent node names. Root nodes map to \code{character(0)} or \code{c()}.}
}
\value{
Invisible \code{NULL}. Raises an error if the DAG is invalid.
}
\description{
Validate a DAG specification
}
\examples{
dag <- list(x = c(), y = c("x"), z = c("x", "y"))
validate_dag(dag)  # silent — DAG is valid
}
